{
  "scripts": {
    "bundle":"parcel build src/index.html",
    "copy": "cp src/pwabuilder-sw.js dist/",
    "build":"npm run bundle && npm run copy"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.4.1",
    "@parcel/transformer-webmanifest": "^2.4.1",
    "parcel": "^2.4.1",
    "postcss": "^8.4.12",
    "tailwindcss": "^3.0.23"
  },
  "dependencies": {
    "@pwabuilder/pwaupdate": "^0.2.0"
  }
}
